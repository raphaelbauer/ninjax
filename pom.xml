<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Parent coordinates -->
    <groupId>org.r10r</groupId>
    <artifactId>ninjax</artifactId>
    <version>0.1.13</version>

    <!-- IMPORTANT: packaging must be pom for a multi‑module parent -->
    <packaging>pom</packaging>

    <name>ninjax-parent</name>
    <url>https://github.com/raphaelbauer/ninjax</url>
    <description>NinjaX is a full stack web framework for Java. Simple. Immutable. Fast and dev friendly.</description>

    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <scm>
        <url>https://github.com/raphaelbauer/ninjax</url>
        <connection>scm:git:git://github.com/raphaelbauer/ninjax</connection>
        <developerConnection>scm:git:ssh://github.com:raphaelbauer/ninjax.git</developerConnection>
    </scm>

    <developers>
        <developer>
            <name>Raphael A. Bauer</name>
            <email>ra@raphaelbauer.com</email>
            <url>http://www.raphaelbauer.com</url>
            <roles>
                <role>Lead Architect</role>
            </roles>
        </developer>
    </developers>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <slf4j.version>2.0.17</slf4j.version>
        <jdbi.version>3.49.5</jdbi.version>
        <jackson.version>2.20.0</jackson.version>
        <junit.version>5.11.0</junit.version>
        <jjwt.version>0.13.0</jjwt.version>
    </properties>

    <!-- Optional: common dependencyManagement (shared versions) -->
    <dependencyManagement>
        <dependencies>
            <!-- Put all your versioned deps here if you want children to inherit versions -->
            <!-- Example (you can move all you had in <dependencies> here): -->
            <dependency>
                <groupId>org.eclipse.jetty</groupId>
                <artifactId>jetty-server</artifactId>
                <version>11.0.25</version>
            </dependency>
            <!-- ...repeat for others as needed... -->
        </dependencies>
    </dependencyManagement>

    <!-- Common build config for all modules -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.14.1</version>
                    <configuration>
                        <source>25</source>
                        <target>25</target>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <!-- Release profile stays on the parent so it applies to all modules -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.9.0</version>
                        <extensions>true</extensions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.8</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.8.0</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <!-- List of child modules (directories under this pom) -->
    <modules>
        <module>ninjax-core</module>
        <!--<module>ninjax-flyway</module>-->
        <!--<module>ninjax-hikari</module>-->
        <!-- add/remove as you need -->
    </modules>
</project>

